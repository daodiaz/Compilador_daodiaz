# programa que escribe una suma
programa tres:
	constantes
		z = 5;
	tipos
		tipoVectorEnteros = vector [1..3] de entero;
	variables
		x,w	: entero;
		h	: tipoVectorEnteros;
comienzo
	x = 0;
	para w en 0 .. 10:
		x = x + 1;
		escribir(x);
	fin para;
	escribir("Bucle terminado");
fin.
